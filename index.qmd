---
title: "plot_diamonds"
format: html
editor_options: 
  chunk_output_type: console
---

```{r}
library(ggplot2)
diamonds |> 
  ggplot(aes(x = carat)) +
  geom_point(aes(y = price), shape = ".") +
  labs(title = Sys.time())
```

